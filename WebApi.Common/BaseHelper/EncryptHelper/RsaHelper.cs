namespace WebApi.Common.BaseHelper.EncryptHelper
{
    /// <summary>
    /// 非对称加密:（其中RSA算法是使用最广泛的非对称加密算法）
    /// 乙方生成两把密钥（公钥和私钥）。公钥是公开的，任何人都可以获得，私钥则是保密的。
    /// 甲方获取乙方的公钥，然后用它对信息加密。
    /// 乙方得到加密后的信息，用私钥解密。
    /// </summary>
    public class RsaHelper
    {
        
    }
}